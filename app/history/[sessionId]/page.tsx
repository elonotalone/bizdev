// Historical sessions mount the exact same workspace runtime as live work.
// The shared session boundary resolves and rehydrates the pathname session id.
export { default } from "../../workspace/page";
